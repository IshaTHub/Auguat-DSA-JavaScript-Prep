function fastSlowPointers(arr){
    let slow = 0;
    let fast = 0;

    while(fast < arr.length){
        //fast pointer moves faster
        fast +=2;
        slow +=1;

        //check condition (cycle, middle, duplicate)
        if(condition met){
            return slow;
        }
    }

    return slow;

}


//[1, 1, 2, 2, 3]

function removeDuplicates(arr){
    let slow = 0;
    
    for(fast = 1; fast < arr.length; fast ++){

    }
    
}